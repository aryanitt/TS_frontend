# Frontend — copy to .env.local for local dev

# Development: leave empty (Vite proxies /api → http://localhost:5000)
# Production (Vercel): set in Project → Settings → Environment Variables
# VITE_API_URL=https://mediumturquoise-capybara-737767.hostingersite.com
VITE_API_URL=
